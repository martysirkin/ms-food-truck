const expect = require('expect');
const routes = require('../routes/routes');

describe('GET /findTrucks', () => {

    describe('#findTrucks', () => {
        it('should insure that there is a client address provided', () => {
            // NO TIME TO FINISH. TESTS ARE STRUCTURED LIKE THIS!
            });
        })
    })
})